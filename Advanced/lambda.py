# lambda - one line, annonymous function, doesn't have a name, defined where you want it to be used 
# assign to a variable - can assign the anonymous function to a variable

func = lambda x, y, r = x + y + r


